#include<iostream>
#include<string>

using namespace std;

int main() {
	string a,b;
    cin >> a;
    cin >> b;
    if (a == b){
        cout <<"two people have the same name";
    }
    else{
        cout <<"two people don't have the same name";
    }
	return 0;
}
